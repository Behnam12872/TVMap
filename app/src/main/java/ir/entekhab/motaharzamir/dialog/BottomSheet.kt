package ir.entekhab.motaharzamir.dialog

class BottomSheet(boolean: Boolean) {
        private var boolean :Boolean=boolean
        fun getbool():Boolean{
            return boolean
        }
        fun setbool(Newoolean:Boolean) {
            boolean = Newoolean
        }
}